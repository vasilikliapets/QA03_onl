import random
from homework_7.1 import bank
from homework_7.2 import code

count = 1


def mod():
    """
модуль, который будет предоставлять возможность выбрать одну из двух предыдущих программ и запустить ее
    """
    var = input('Выберите программу: 1 - Шифр цезаря, 2 - Банковский вклад ')
    if var == '1':
        code()
        var2 = input('Для запуска другой программы введите 1')
        if var2 == '1':
            bank(a: int, years: int)
        else:
            print('Программа завершена')
    elif var == '2':
        bank(a: int, years: int)
        var2 = input('Для запуска другой программы введите 1: ')
        if var2 == '1':
            code()
        else:
            print('Программа завершена')
    else:
        global count
        if count < 3:
            print('Неправильно выбрана программа!')
            count += 1
            mod()
        else:
            print('Запущена программа, выбранная случайно')
            prog = [code, bank]
            rand_prog = random.choice(prog)
            rand_prog()


mod()
